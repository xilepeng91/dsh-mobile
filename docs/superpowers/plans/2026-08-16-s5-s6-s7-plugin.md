# S5 内容可视化 + S6 文件工作区 + S7 扩展运维 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **流程简化（用户决定，S4 沿用）：** 不做代码审计/评审（无 task review、无 final review、无 fix loop）。每个任务 = 实现 + 测试 + 全流程跑通（App：`gradlew :app:testDebugUnitTest` 全绿 + `assembleDebug` 成功；插件：`node --check`）即门禁。实现者自测即门禁，逐任务独立 commit。

**Goal:** DSH Remote 能力补全三阶段（依据 `.superpowers/sdd/2026-08-16-s5-s6-s7-plugin/recon-report.md` 只读侦察）：S5 内容可视化（思考链展示、工具调用耗时、MCP 服务列表）、S6 文件工作区（目录列举直连、文件只读预览）、S7 扩展运维（slash 命令面板、日志本地导出）。

**Architecture:** App 直连能力（思考链/工具耗时/slash 面板/日志导出/目录列举）优先——纯解析/计算逻辑抽顶层纯函数 + JVM 单测，UI 增量接入；PC 插件 `dsh-remote-access` 仅新增两个只读路由（`mcp/list`、`fs/read` + 增强 `fs/list` 列文件），全部走已有 ctx 能力与沙箱读取，不扩展权限语义。

**Tech Stack:** Kotlin 2.1.21 / Compose BOM 2025.06 / kotlinx-coroutines-test 1.10.2 / JUnit 4.13.2；PC 插件 Node 20（dsh-remote-access，webServer.register + ctx.inject(["fs"])/ctx.tools）。

**Spec / 现状依据:** `.superpowers/sdd/2026-08-16-s5-s6-s7-plugin/recon-report.md`（只读侦察，含全部 file:line 证据与候选路由形状；行号如漂移以 grep 为准）

## Global Constraints

- **测试命令统一在 `C:\hremote`（ASCII junction → 仓库）下执行**，且须先设 `$env:GRADLE_USER_HOME = "C:\hremote\.gradle-home"`（沙箱禁止写 `~/.gradle`；该目录含已复制的发行版与缓存）。
  ```
  cd C:\hremote
  $env:GRADLE_USER_HOME = "C:\hremote\.gradle-home"
  .\gradlew.bat :app:testDebugUnitTest --offline
  .\gradlew.bat :app:assembleDebug --offline
  ```
- **不做评审**：实现者自测（单测全绿 + assembleDebug / node --check）即完成门禁。每个任务单独 `git add <本任务文件>` + 单独 commit，禁止 `git add -A` / `git commit -a`。
- **范围红线（安全裁定，不做）**：文件写入/删除、通用文件上传、真终端 shell（均绕过 DSH sandbox/权限语义）；S7 版本项（`host.describe` 硬编码在 DSH 上游 `@deepseek-ai/dsh`，不在本仓库可控范围）。
- **并行改动**：S4 已完成并恢复（版本 1.3.1 + 超长消息折叠已提交 `15b4043`）。当前工作树 app/ 干净，无保护文件。`SessionScreen.kt`/`HomeScreen.kt`/`DshConnection.kt` 为多任务共享文件，任务串行执行。
- 插件改动（Task 4/7）在 `dsh-remote-access/` 内：新增路由挂 `ctx.effect`（S3 已确立模式），`node --check lib/index.js` + `node --check lib/client.js` 通过；插件 README 更新日志。
- 每任务把可测逻辑抽成顶层/internal 纯函数 + JVM 单测；纯 UI 无逻辑处不强求单测，以 assembleDebug 通过为准。

---

### Task 1: S5 思考链展示（App 直连）

**Files:**
- Modify: `app/src/main/java/com/dsh/mobile/ui/screens/SessionScreen.kt`（chunk 合并 :184-186 附近的 `chunkTextOf`、ChatItem 模型 :94-109、AssistantCard 渲染、状态区）
- Modify: `app/src/main/java/com/dsh/mobile/data/DshProtocol.kt`（如需要：从宽松 JsonElement 提取思考文本的纯函数放这里）
- Test: `app/src/test/java/com/dsh/mobile/data/ThinkingExtractTest.kt`（新增）

**Interfaces:**
- 顶层纯函数（`data/ThinkingExtract.kt` 同包 internal）：
```kotlin
/** 从 assistant/chunk 事件 data 提取思考增量：data.chunk.type == "reasoning-delta" → chunk.text，否则 null */
fun reasoningDeltaOf(eventData: JsonElement?): String?
/** 从 assistant/message 事件 data 提取完整思考：data.message.content[] 中 type == "reasoning" 的块文本拼接 */
fun reasoningBlockOf(eventData: JsonElement?): String?
```

- [ ] **Step 1: 写失败测试** `ThinkingExtractTest.kt`：reasoningDeltaOf（reasoning-delta→文本；正文 chunk→null；缺 chunk/缺 type→null）；reasoningBlockOf（含 reasoning 块→拼接；无 reasoning 块→null；多块→顺序拼接）。
- [ ] **Step 2: 实现纯函数**。
- [ ] **Step 3: SessionScreen 接线**：`chunkTextOf` 现丢弃 reasoning-delta（:184-186 注释）——改为流式时把 `reasoningDeltaOf` 结果累积到 ChatItem.Assistant 的 `thinkingText` 字段（模型加 `val thinkingText: String = ""`，默认参数不破坏既有构造）；`assistant/message` 完整消息到达时用 `reasoningBlockOf` 兜底（若流式已累积则取长/合并）。UI：AssistantCard 在内容上方渲染「💭 已思考」折叠区（点击展开/收起思考文本，纯文本样式 `bodySmall` + 弱色，不渲染 Markdown），流式进行中显示「思考中…」。注意与既有「已思考 x 秒」占位（README 描述）并存不冲突。
- [ ] **Step 4: 校验**（`cd C:\hremote` + GRADLE_USER_HOME）：`.\gradlew.bat :app:testDebugUnitTest --offline`（全绿）→ `.\gradlew.bat :app:assembleDebug --offline`（成功）。
- [ ] **Step 5: Commit**
```bash
git add app/src/main/java/com/dsh/mobile/data/ThinkingExtract.kt app/src/main/java/com/dsh/mobile/data/DshProtocol.kt app/src/main/java/com/dsh/mobile/ui/screens/SessionScreen.kt app/src/test/java/com/dsh/mobile/data/ThinkingExtractTest.kt
git commit -m "feat(s5): 思考链展示——reasoning-delta 累积 + 折叠思考区"
```

---

### Task 2: S5 工具调用耗时（App 直连）

**Files:**
- Modify: `app/src/main/java/com/dsh/mobile/ui/screens/SessionScreen.kt`（事件分发、ChatItem.Tool 模型、ToolCard 渲染）
- Test: `app/src/test/java/com/dsh/mobile/data/ToolTimingTest.kt`（新增）

**Interfaces:**
- 顶层纯函数（`data/ToolTiming.kt` 同包 internal）：
```kotlin
/** 工具耗时：从 wire 事件信封取 time。维护 callId→startTime 映射，配对 tool/result 计算耗时 ms；无配对/负值→null */
class ToolTimingTracker {
    fun onCall(callId: String, timeMs: Long)
    fun onResult(toolCallId: String, timeMs: Long): Long?   // 计算并移除条目
    fun cleanup()                                           // 清空（断线/换会话）
}
```
- ChatItem.Tool 增加 `val elapsedMs: Long? = null`（默认参数）。

- [ ] **Step 1: 写失败测试** `ToolTimingTest.kt`：正常配对（result-call 差值）；无 call 先 result→null 不崩；重复 call 覆盖；重复 result→null；负值→null；cleanup 清空。
- [ ] **Step 2: 实现纯函数**。
- [ ] **Step 3: SessionScreen 接线**：事件分发处（tool/call → tracker.onCall(callId, wire.time)；tool/result → 从 `message.content[0].toolCallId` 取 id 配对 → elapsedMs 写入 ChatItem.Tool）；断线/换会话时 tracker.cleanup()（在重连/会话切换处）。ToolCard 渲染：有 elapsedMs 时在状态徽章旁显示「耗时 x.x s」（`%.1f` 格式化，1s 以下显示 ms）。
- [ ] **Step 4: 校验**（`cd C:\hremote` + GRADLE_USER_HOME）：testDebugUnitTest 全绿 → assembleDebug 成功。
- [ ] **Step 5: Commit**
```bash
git add app/src/main/java/com/dsh/mobile/data/ToolTiming.kt app/src/main/java/com/dsh/mobile/ui/screens/SessionScreen.kt app/src/test/java/com/dsh/mobile/data/ToolTimingTest.kt
git commit -m "feat(s5): 工具调用耗时——callId 配对 + 徽章展示"
```

---

### Task 3: S6 目录列举直连 host.listDirectory（App）

**Files:**
- Modify: `app/src/main/java/com/dsh/mobile/data/DshConnection.kt`（新增 `listDirectory` 走 host RPC，:499-530 附近；现有插件 fs/list 路径保留为回退）
- Modify: `app/src/main/java/com/dsh/mobile/data/DshProtocol.kt`（新增响应模型/解析纯函数）
- Modify: `app/src/main/java/com/dsh/mobile/ui/screens/HomeScreen.kt`（工作区面板「浏览电脑目录」:268-342 workspaceSheetOverlay 区域适配新模型）
- Test: `app/src/test/java/com/dsh/mobile/data/DirectoryListTest.kt`（新增）

**Interfaces:**
- 纯函数（`data/DirectoryList.kt` 同包 internal）：
```kotlin
/** 解析 host.listDirectory 响应：{path, home, crumbs[], entries[{name,path,hidden}], truncated} → 扁平模型 */
data class DirEntry(val name: String, val path: String, val hidden: Boolean)
data class DirListing(val path: String, val crumbs: List<String>, val entries: List<DirEntry>, val truncated: Boolean)
fun parseDirectoryList(data: JsonElement?): DirListing?   // 非法→null
```

- [ ] **Step 1: 写失败测试** `DirectoryListTest.kt`：正常解析（crumbs/entries/truncated）；缺字段容错；非法 JSON→null；空 entries。
- [ ] **Step 2: 实现纯函数**。
- [ ] **Step 3: DshConnection 接线**：新增 `suspend fun listDirectory(path: String?): DirListing`——优先调 `host.listDirectory` RPC（若协议支持），失败回退现有插件 `fs/list` 路径转换（现有 :622-641 的 listWorkspaceDirs 保留不破坏）。DshProtocol 增加对应 RPC 常量/请求构建（照抄现有 unary 调用模式）。
- [ ] **Step 4: HomeScreen 工作区面板**：`workspaceSheetOverlay` 的目录浏览从「只列子目录」升级为 `listDirectory`（crumbs 面包屑显示当前路径层级、entries 含隐藏项、truncated 提示「结果已截断」）；进/退导航逻辑适配新模型；选中目录创建工作区的行为不变。
- [ ] **Step 5: 校验**（`cd C:\hremote` + GRADLE_USER_HOME）：testDebugUnitTest 全绿 → assembleDebug 成功。
- [ ] **Step 6: Commit**
```bash
git add app/src/main/java/com/dsh/mobile/data/DirectoryList.kt app/src/main/java/com/dsh/mobile/data/DshProtocol.kt app/src/main/java/com/dsh/mobile/data/DshConnection.kt app/src/main/java/com/dsh/mobile/ui/screens/HomeScreen.kt app/src/test/java/com/dsh/mobile/data/DirectoryListTest.kt
git commit -m "feat(s6): 目录列举直连 host.listDirectory——crumbs/隐藏项/截断提示"
```

---

### Task 4: S6 文件内容只读预览（PC 插件路由 + App）

**Files:**
- Modify: `dsh-remote-access/lib/index.js`（新增 `GET /api/remote-access/fs/read`；增强现有 `fs/list` 返回 `files[]`）
- Modify: `dsh-remote-access/README.md`（更新日志 + 路由说明）
- Modify: `app/src/main/java/com/dsh/mobile/data/DshConnection.kt`（新增 `fsRead`/`fsList` 调用）
- Modify: `app/src/main/java/com/dsh/mobile/ui/screens/HomeScreen.kt`（目录条目含文件；文件点击→预览对话框）
- Test: `app/src/test/java/com/dsh/mobile/data/FilePreviewTest.kt`（新增，App 侧解析纯函数）

**Interfaces:**
- 插件路由 `GET /api/remote-access/fs/read?path=<abs>`：
  ```json
  { "ok": true, "path": "<abs>", "size": 12345, "truncated": false, "isBinary": false, "text": "..." }
  ```
  - 实现：优先 `ctx.inject(["fs"])` 的 `ctx.fs.readText`（尊重工作区/sandbox 语义）；不可用时回退 `node:fs.readFileSync`；>1MB 截断（`truncated: true`）；非 UTF-8/含 NUL → `isBinary: true` + base64 返回 `data` 字段。
  - 挂载：路由注册走既有 `ctx.effect` 生命周期（S3 模式）。
- 插件 `fs/list` 增强：响应加 `files: [{name, path, size, hidden}]`（保留 `dirs[]` 兼容旧 App）。
- App 纯函数（`data/FilePreview.kt` 同包 internal）：
```kotlin
data class FilePreview(val path: String, val size: Long, val truncated: Boolean, val isBinary: Boolean, val text: String?)
fun parseFilePreview(data: JsonElement?): FilePreview?   // ok=false/非法→null
```

- [ ] **Step 1: 写失败测试** `FilePreviewTest.kt`：正常文本；binary 分支（text=null,data 存在）；truncated；ok=false→null。
- [ ] **Step 2: 实现 App 纯函数**。
- [ ] **Step 3: 插件实现 fs/read + fs/list 增强**（`node --check lib/index.js` 通过；保留既有路由与行为）。
- [ ] **Step 4: App 接线**：`DshConnection.fsRead(path)`/`fsList(path)`；HomeScreen 工作区面板：`listDirectory` 结果含文件时文件行可点击 → 预览对话框（文本：等宽 bodySmall 滚动展示 + 截断提示；二进制：显示「二进制文件（size KB）」+ base64 大小；对话框可关闭/复制文本）。
- [ ] **Step 5: 校验**：`cd C:\hremote` + GRADLE_USER_HOME：testDebugUnitTest 全绿 → assembleDebug 成功；插件 `cd dsh-remote-access && node --check lib/index.js`。
- [ ] **Step 6: Commit（分两个：插件 + App）**
```bash
git add dsh-remote-access/lib/index.js dsh-remote-access/README.md
git commit -m "feat(s6): 插件 fs/read 只读预览 + fs/list 列文件"
git add app/src/main/java/com/dsh/mobile/data/FilePreview.kt app/src/main/java/com/dsh/mobile/data/DshConnection.kt app/src/main/java/com/dsh/mobile/ui/screens/HomeScreen.kt app/src/test/java/com/dsh/mobile/data/FilePreviewTest.kt
git commit -m "feat(s6): 文件预览对话框——文本展示/二进制提示/复制"
```

---

### Task 5: S7 slash 命令面板（App）

**Files:**
- Modify: `app/src/main/java/com/dsh/mobile/data/DshConnection.kt`（新增 `commandsList()`，:548-553 已有 executeCommand）
- Modify: `app/src/main/java/com/dsh/mobile/ui/screens/SessionScreen.kt`（入口 + 面板 UI）
- Test: `app/src/test/java/com/dsh/mobile/data/CommandsListTest.kt`（新增）

**Interfaces:**
- 纯函数（`data/CommandsList.kt` 同包 internal）：
```kotlin
data class SlashCommand(val name: String, val description: String?, val hint: String?)
fun parseCommandsList(data: JsonElement?): List<SlashCommand>   // 非法→空列表
```

- [ ] **Step 1: 写失败测试** `CommandsListTest.kt`：正常解析（name/description/input.hint）；空列表；非法→空；缺字段容错。
- [ ] **Step 2: 实现纯函数 + DshConnection.commandsList()**（`commands/list` RPC，照 executeCommand 模式）。
- [ ] **Step 3: SessionScreen 面板**：顶栏菜单/工具卡片区加「/ 命令」入口 → 底部弹出面板（ModalBottomSheet 或 AlertDialog）：列表显示 `name` + `description`（hint 作占位提示），点击 → `executeCommand(agentId, line=name)` 发送，结果 Toast/轻提示（kind error 红字）；面板可关闭。样式对齐项目既有对话框（SkillsDialog 模式）。
- [ ] **Step 4: 校验**（`cd C:\hremote` + GRADLE_USER_HOME）：testDebugUnitTest 全绿 → assembleDebug 成功。
- [ ] **Step 5: Commit**
```bash
git add app/src/main/java/com/dsh/mobile/data/CommandsList.kt app/src/main/java/com/dsh/mobile/data/DshConnection.kt app/src/main/java/com/dsh/mobile/ui/screens/SessionScreen.kt app/src/test/java/com/dsh/mobile/data/CommandsListTest.kt
git commit -m "feat(s7): slash 命令面板——commands/list + 执行"
```

---

### Task 6: S7 日志本地导出（App）

**Files:**
- Modify: `app/src/main/java/com/dsh/mobile/data/DshConnection.kt`（复用 history）
- Modify: `app/src/main/java/com/dsh/mobile/ui/screens/SessionScreen.kt`（导出入口 + 分享）
- Modify: `app/src/main/AndroidManifest.xml`（如需要：FileProvider 已有用于安装器，分享走 ACTION_SEND + cacheDir 临时文件即可，可能无需改 Manifest）
- Test: `app/src/test/java/com/dsh/mobile/data/SessionExportTest.kt`（新增）

**Interfaces:**
- 纯函数（`data/SessionExport.kt` 同包 internal）：
```kotlin
/** 把 session.history 的原始 events[]（List<JsonElement>）转成可读 Markdown/JSON 文本 */
fun historyToMarkdown(sessionTitle: String?, events: List<JsonElement>): String
fun historyToJson(events: List<JsonElement>): String
```

- [ ] **Step 1: 写失败测试** `SessionExportTest.kt`：Markdown（标题行、用户/助手消息分节、工具调用缩进、空 events）；JSON（合法序列化、round-trip）。
- [ ] **Step 2: 实现纯函数**。
- [ ] **Step 3: SessionScreen 导出入口**：顶栏菜单（与复制/重命名同区）加「导出会话」→ 拉 `history(sessionId)` 原始 events → `historyToMarkdown` → 写 `context.cacheDir/exports/<sessionId>.md` → `ACTION_SEND`（text/markdown，FileProvider URI 或 content:// 分享）；失败 Toast。分享后保留文件或清理（保留，供多次分享）。
- [ ] **Step 4: 校验**（`cd C:\hremote` + GRADLE_USER_HOME）：testDebugUnitTest 全绿 → assembleDebug 成功。
- [ ] **Step 5: Commit**
```bash
git add app/src/main/java/com/dsh/mobile/data/SessionExport.kt app/src/main/java/com/dsh/mobile/ui/screens/SessionScreen.kt app/src/test/java/com/dsh/mobile/data/SessionExportTest.kt
git commit -m "feat(s7): 会话日志本地导出——Markdown 分享"
```

---

### Task 7: S5 MCP 服务列表（PC 插件路由 + App 展示）

**Files:**
- Modify: `dsh-remote-access/lib/index.js`（新增 `GET /api/remote-access/mcp/list`）
- Modify: `dsh-remote-access/README.md`（更新日志）
- Modify: `app/src/main/java/com/dsh/mobile/data/DshConnection.kt`（新增 `mcpList()`）
- Modify: `app/src/main/java/com/dsh/mobile/ui/screens/SettingsScreen.kt`（「关于/安全」区加 MCP 服务卡片）
- Test: `app/src/test/java/com/dsh/mobile/data/McpListTest.kt`（新增）

**Interfaces:**
- 插件路由 `GET /api/remote-access/mcp/list`：
  ```json
  { "ok": true, "servers": [ { "serverName": "github", "tools": ["mcp__github__search_repos"], "status": "unknown" } ] }
  ```
  - 实现：`ctx.inject(["tools"])` 拿 `ctx.tools.schemas()`（或既有 ctx.tools），按 `mcp__<server>__<tool>` 前缀聚合 serverName → tools[]；status 恒 `"unknown"`（上游无连接态 API，侦察 §2.2 已确认）；挂 ctx.effect。`node --check` 通过。
- App 纯函数（`data/McpList.kt` 同包 internal）：
```kotlin
data class McpServer(val serverName: String, val tools: List<String>, val status: String)
fun parseMcpList(data: JsonElement?): List<McpServer>   // ok=false/非法→空
```

- [ ] **Step 1: 写失败测试** `McpListTest.kt`：正常解析；无 mcp 工具→空 servers；非法→空；status 缺省→"unknown"。
- [ ] **Step 2: 实现 App 纯函数**。
- [ ] **Step 3: 插件实现 mcp/list**（`node --check lib/index.js` 通过）。
- [ ] **Step 4: App 接线**：`DshConnection.mcpList()`；SettingsScreen「关于」区（或「安全」下）加「MCP 服务」卡片：连接时显示 servers 列表（serverName + 工具数徽章；status 为 unknown 不显示或显示「连接状态未知」）；未连接/空→「无 MCP 服务」。样式对齐既有卡片。
- [ ] **Step 5: 校验**：testDebugUnitTest 全绿 → assembleDebug 成功；`node --check lib/index.js`。
- [ ] **Step 6: Commit（分两个：插件 + App）**
```bash
git add dsh-remote-access/lib/index.js dsh-remote-access/README.md
git commit -m "feat(s5): 插件 mcp/list 路由——MCP 服务与工具枚举"
git add app/src/main/java/com/dsh/mobile/data/McpList.kt app/src/main/java/com/dsh/mobile/data/DshConnection.kt app/src/main/java/com/dsh/mobile/ui/screens/SettingsScreen.kt app/src/test/java/com/dsh/mobile/data/McpListTest.kt
git commit -m "feat(s5): MCP 服务列表卡片"
```

---

## 收尾（S5-S7 完成门禁）

- [ ] 全量回归（`cd C:\hremote` + GRADLE_USER_HOME）：`.\gradlew.bat :app:testDebugUnitTest --offline` 全绿 + `.\gradlew.bat :app:assembleDebug --offline` 成功 + 插件 `node --check lib/index.js` 通过 + `git status` app/ 干净。
- [ ] README.md 更新日志追加「S5-S7」条目（v?.?.? 占位，样式照抄 S4）。
- [ ] 提交 README 更新（独立 commit）。
- [ ] 进度记录写入 `.superpowers/sdd/2026-08-16-s5-s6-s7-plugin/progress.md`。

## Self-Review 记录（计划作者自查）

- **Spec 覆盖**：T1 思考链→Task 1；T2 工具耗时→Task 2；T3 目录列举→Task 3；T4 文件预览→Task 4；T5 slash 面板→Task 5；T6 日志导出→Task 6；T7 MCP 列表→Task 7。侦察报告「App 可独立做」5 项全部覆盖，「必须插件」2 项（mcp/list、fs/read）覆盖，「建议跳过」3 项（写/删/上传/真终端）明确排除。
- **占位符扫描**：无 TBD/TODO；MCP status 恒 "unknown" 已在计划注明（上游无连接态 API）。
- **类型一致性**：`reasoningDeltaOf/reasoningBlockOf`（T1）；`ToolTimingTracker`（T2）；`parseDirectoryList`（T3）；`parseFilePreview`（T4）；`parseCommandsList`（T5）；`historyToMarkdown/historyToJson`（T6）；`parseMcpList`（T7）。均为独立纯函数，无跨任务类型依赖。
- **执行注意**：SessionScreen.kt 被 T1/T2/T5/T6 触及、HomeScreen.kt 被 T3/T4 触及、DshConnection.kt 被 T3/T4/T5/T6/T7 触及——全部串行执行，禁止并行实现者。插件改动（T4/T7）在 dsh-remote-access/ 内与 App 改动同任务但分两个 commit。所有 gradle 命令带 GRADLE_USER_HOME。并行改动保护已解除（S4 恢复完毕）。
