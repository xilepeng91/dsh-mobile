# S9 遗留清理二期 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **流程简化（用户决定，S4-S8 沿用）：** 不做代码审计/评审。每个任务 = 实现 + 测试 + 全流程跑通（`gradlew :app:testDebugUnitTest` 全绿 + `assembleDebug` 成功）即门禁。实现者自测即门禁，逐任务独立 commit。

**Goal:** 清理 S2/S3 遗留 parked 项与通知残留：①墓碑 clear 全量清空改增量裁剪 + recover 查墓碑；②生物锁 unknown 占位补触摸拦截；③审批/问答通知残留修复（resolved 时 cancel 对应通知）；④PC 插件部署到 profile 双副本（S5-S7 新路由生效）。

**Architecture:** 全部为 App 端逻辑修复 + 一项插件部署（文件复制，非代码改动）：
- ApprovalCenter：`resolvedTombstones` 超限改「删最旧」（LinkedHashSet/队列语义）而非全清；`recover()` 合并时查墓碑。
- MainActivity：unknown 占位 Box 加 `clickable(enabled=true){}` 或等效消费触摸。
- DshConnectionService：维护 `sessionId → 通知 id` 映射，`ApprovalResolved`/`QuestionResolved` 时 `manager.cancel` 对应通知。

**Tech Stack:** Kotlin 2.1.21 / kotlinx-coroutines-test / JUnit 4.13.2；插件部署 = 文件复制 + node --check。

**Spec / 现状依据:** S2/S3 ledger parked 项（已核实代码现状）；部署目标 `C:\Users\Administrator\.dsh\profiles\web\node_modules\@deepseek-ai\dsh-remote-access\lib\`（S3/S5-S7 记录「profile 两处 lib 副本」——实际以 glob 为准）

## Global Constraints

- **测试命令统一在 `C:\hremote`（ASCII junction → 仓库）下执行**，且须先设 `$env:GRADLE_USER_HOME = "C:\hremote\.gradle-home"`。
- **不做评审**：实现者自测（单测全绿 + assembleDebug）即完成门禁。每个任务单独 `git add <本任务文件>` + 单独 commit，禁止 `git add -A` / `git commit -a`。
- 任务串行执行（ApprovalCenter.kt 被 T1 触及、MainActivity.kt 被 T2 触及、DshConnectionService.kt 被 T3 触及）。
- T4 为部署任务（复制文件到 profile），不产生仓库 commit；部署前 `node --check`，部署后不重启 DSH（重启会中断当前会话，由用户操作）。

---

### Task 1: 墓碑增量裁剪 + recover 查墓碑

**Files:**
- Modify: `app/src/main/java/com/dsh/mobile/data/ApprovalCenter.kt`（`resolvedTombstones` 处理 :88-102、`recover()` :120-133）
- Modify: `app/src/test/java/com/dsh/mobile/data/ApprovalCenterTest.kt`（新增用例）

**现状缺陷（S2 parked）：**
1. `if (resolvedTombstones.size > 200) resolvedTombstones.clear()`（:90/:100）全量清空——>200 个已解决项后墓碑全丢，历史已解决项在重连重扫时可回加。
2. `recover()`（:127-129）合并时 `merged.none { itemKey(it) == itemKey(item) }` 不查墓碑——重连重扫可回加历史已解决项。

**Interfaces:**
- 新增顶层/internal 纯函数（同文件或 `data/TombstonePolicy.kt`）：
```kotlin
/** 墓碑超限裁剪：保留最近 200 条（按加入顺序），丢弃最旧——与全清相比不丢近期已解决项 */
internal fun trimTombstones(current: Set<String>, capacity: Int = 200): Set<String>
```

- [ ] **Step 1: 写失败测试**：`trimTombstones`——超限裁剪保留最近 200（加入序）；未超限原样；空集；容量 0。`recover()` 合并查墓碑——已解决（在墓碑）的历史审批/问答不被回加；未解决正常回加；测试用假 listSessionsFn/historyFn 构造恢复场景（照既有 ApprovalCenterTest 的 fake 模式）。
- [ ] **Step 2: 实现 `trimTombstones`**（有序集合 LinkedHashSet，超限时逐删最旧；或按 size 裁剪）。
- [ ] **Step 3: ApprovalCenter 接线**：:90/:100 的 `resolvedTombstones.clear()` 改为 `resolvedTombstones = trimTombstones(resolvedTombstones)`（注意 `resolvedTombstones` 声明形态——若为 `MutableSet` 则用 retainAll/remove 最旧，或以不可变替换；以实际代码为准）；`recover()` 合并处加 `itemKey(item) !in resolvedTombstones` 条件。
- [ ] **Step 4: 校验**（`cd C:\hremote` + GRADLE_USER_HOME）：testDebugUnitTest 全绿 → assembleDebug 成功。
- [ ] **Step 5: Commit**
```bash
git add app/src/main/java/com/dsh/mobile/data/ApprovalCenter.kt app/src/test/java/com/dsh/mobile/data/ApprovalCenterTest.kt
git commit -m "fix(s2): 墓碑增量裁剪 + recover 查墓碑——重连不再回加已解决项"
```

---

### Task 2: 生物锁 unknown 占位拦截触摸

**Files:**
- Modify: `app/src/main/java/com/dsh/mobile/MainActivity.kt`（:250-254 unknown 占位 Box）

**现状缺陷（S3 parked minor）：** `"unknown" -> Box(Modifier.fillMaxSize().background(...))`——占位不消费触摸，冷启动异步读开关的短暂窗口内内容层仍可交互（视觉已遮蔽但触摸可穿透）。

- [ ] **Step 1: 修复**：占位 Box 加 `clickable(enabled = true, onClick = {})`（或 `.pointerInput(Unit) {}` 消费触摸）——阻断穿透；保持背景不透明。
- [ ] **Step 2: 校验**：`cd C:\hremote` + GRADLE_USER_HOME：testDebugUnitTest 全绿（不回归）→ assembleDebug 成功。（无纯逻辑可单测，UI 行为由编译 + assembleDebug 覆盖。）
- [ ] **Step 3: Commit**
```bash
git add app/src/main/java/com/dsh/mobile/MainActivity.kt
git commit -m "fix(s3): unknown 锁占位拦截触摸——冷启动窗口不可点穿"
```

---

### Task 3: 审批/问答通知残留修复

**Files:**
- Modify: `app/src/main/java/com/dsh/mobile/service/DshConnectionService.kt`（`postAlert` :238-263、`ApprovalResolved` :176、`QuestionResolved` :196、`handle` 前置 :150-151、主机切换清理 :110-117）
- Test: 无纯函数可测（UI/服务层），以编译 + assembleDebug 为门禁；如需可抽纯函数（如通知 id 分配）——不强求。

**现状缺陷（S2 终审 Important #3，暂缓跟进）：** `postAlert` 用 `1000 + (notificationSeq++ % 100)` 循环 id（:263）；`ApprovalResolved`/`QuestionResolved` 只清 `seenApprovals`/`seenQuestions` 集合（:176/:196），**不 cancel 已发的横幅通知**——审批/问答解决后通知仍留在托盘。

**Interfaces:**
- Service 内新增：`private val notificationIds = mutableMapOf<String, Int>()`（key = 审批 `sessionId:approvalId` / 问答 `sessionId`，value = 通知 id）。
- `postAlert` 增加参数 `trackKey: String? = null`：非空时记录 `notificationIds[trackKey] = 当前通知 id`。
- 审批横幅（:160-165）传 `trackKey = "a:${event.sessionId}:${event.approvalId}"`；问答横幅（:168-177）传 `trackKey = "q:${event.sessionId}"`。
- `ApprovalResolved`：`notificationIds.remove("a:${sessionId}:${approvalId}")?.let { manager.cancel(it) }`（需取 NotificationManager——postAlert 内已有取法，抽 private fun 或复用 getSystemService）。
- `QuestionResolved`：同理 `"q:${sessionId}"`。
- 主机切换清理（:110-117 区域）追加 `notificationIds.clear()`。

- [ ] **Step 1: 实现**（按 Interfaces 接线；NotificationManager 取用对齐 postAlert 内写法）。
- [ ] **Step 2: 校验**：`cd C:\hremote` + GRADLE_USER_HOME：testDebugUnitTest 全绿 → assembleDebug 成功。
- [ ] **Step 3: Commit**
```bash
git add app/src/main/java/com/dsh/mobile/service/DshConnectionService.kt
git commit -m "fix(s2): 审批/问答解决后取消对应横幅通知——托盘不再残留"
```

---

### Task 4: PC 插件部署到 profile 双副本

**Files:**
- 源：`dsh-remote-access/lib/index.js`、`lib/client.js`、`package.json`（仓库内，已含 S5-S7 新路由）
- 目标：`C:\Users\Administrator\.dsh\profiles\web\node_modules\@deepseek-ai\dsh-remote-access\`（及可能第二处，glob 确认）

**背景：** S3/S5-S7 记录「插件需部署到 profile 两处 lib 副本 + 重启 DSH 才生效」。S5-S7 新增 fs/read、fs/list 列文件、mcp/list 路由——部署后 App 端新功能才能联调。

- [ ] **Step 1: 确认部署目标**：glob `C:\Users\Administrator\.dsh\profiles\web\node_modules\@deepseek-ai\dsh-remote-access\` 与可能的第二副本；记录当前 lib/index.js 是否含新路由（grep fs/read/mcp/list）。
- [ ] **Step 2: 复制文件**：仓库 `dsh-remote-access/lib/index.js`、`lib/client.js` 复制覆盖到目标 lib/（保留其他文件不动）。
- [ ] **Step 3: 校验**：`node --check` 目标副本 index.js/client.js；grep 确认 fs/read、mcp/list 路由在目标副本中。
- [ ] **Step 4: 不产生仓库 commit**；**不重启 DSH**（重启会中断当前会话，由用户操作）；在报告与最终总结中明确「需用户重启 DSH 后新路由生效」。

## 收尾（S9 完成门禁）

- [ ] 全量回归（`cd C:\hremote` + GRADLE_USER_HOME）：`.\gradlew.bat :app:testDebugUnitTest --offline` 全绿 + `.\gradlew.bat :app:assembleDebug --offline` 成功 + `git status` app/ 干净。
- [ ] 进度记录写入 `.superpowers/sdd/2026-08-16-s8-cleanup/progress.md`（S9 段落）或新建 `2026-08-16-s9-cleanup2/progress.md`。

## Self-Review 记录（计划作者自查）

- **覆盖**：S2 parked 墓碑 → T1；S3 parked unknown 触摸 → T2；S2 终审 Important #3 通知残留 → T3；S5-S7 插件生效 → T4。S2 另一 parked（runBatch busy）经核实**代码已重构无 busy 标志，该项过时跳过**。
- **类型一致性**：`trimTombstones`（T1）；通知 id 映射（T3，key 格式与 itemKey 同构）。T2/T4 无纯逻辑。
- **执行注意**：ApprovalCenter.kt 仅 T1；MainActivity.kt 仅 T2；DshConnectionService.kt 仅 T3——串行无冲突。T4 为部署（无 commit）。所有 gradle 命令带 GRADLE_USER_HOME。T4 需访问 profile 目录（沙箱可能限制——若被拒按沙箱规则处理，部署可由用户执行并记录路径）。
